# Local development only (`streamlit run`). Cluster config comes from lte-apps-helm
# deploy/helm/billing-hours-calculator values.yaml → streamlitConfig (ConfigMap).

[theme]
base = "light"
primaryColor = "#2C7BE5"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F5F7FB"
textColor = "#000000"

[browser]
gatherUsageStats = false
